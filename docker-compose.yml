---

services:
  bjoern:
    build:
      context: .
      dockerfile: bjoern.Dockerfile
    ports:
      - "8000:8000"
    volumes:
    - .:/build
    cpu_count: 1

  granian:
    build:
      context: .
      dockerfile: granian.Dockerfile
    ports:
    - "8001:8000"
    volumes:
    - .:/build
    cpu_count: 1

  gunicorn:
    build:
      context: .
      dockerfile: gunicorn.Dockerfile
    ports:
    - "8002:8000"
    volumes:
    - .:/build
    cpu_count: 1

  uvicorn:
    build:
      context: .
      dockerfile: uvicorn.Dockerfile
    ports:
    - "8003:8000"
    volumes:
    - .:/build
    cpu_count: 1
